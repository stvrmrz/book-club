<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    {{> navbar}}
    <h1>Welcome to Book Club Manager</h1>
    <p>Your go-to platform for managing book clubs, reading lists, meetings, and discussions.</p>
    <hr>
    <h2>Search for Books</h2>
    <form action="/books/search" method="GET">
        <input type="text" name="query" placeholder="Search for books" required>
        <button type="submit">Search</button>
    </form>
    <hr>
    <h2>Featured Clubs</h2>
    {{#each clubs}}
        <div class="club">
            <h3>{{this.name}}</h3>
            <p>{{this.description}}</p>
            <a class="button" href="/clubs/{{this.id}}">View Club</a>
        </div>
    {{/each}}
</body>
</html>
